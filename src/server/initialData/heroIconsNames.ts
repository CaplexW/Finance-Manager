// import * as icons from '@heroicons/react/24/solid';
// import { IHeroIconName } from '../models/heroIconName.ts';

// const names = Object.keys(icons);
// const heroIconsNames: IHeroIconName[] = names.map((name) => ({ name }));

// export default heroIconsNames;
