// import { Schema, Types, model } from "mongoose";

// const schema = {
//   name: { type: String, required: true },
// };

// const icon = new Schema(schema, { timestamps: true });
// const HeroIconName = model('HeroIconName', icon);

// export default HeroIconName;

// export interface IHeroIconName {
//   _id?: Types.ObjectId,
//   name: string,
// }
